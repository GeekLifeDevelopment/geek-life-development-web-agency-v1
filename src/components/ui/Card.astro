---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<article
  class={`rounded-xl border border-slate-100 bg-white p-6 shadow-soft transition duration-200 hover:-translate-y-0.5 ${className}`}
>
  <slot />
</article>