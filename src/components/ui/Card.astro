---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<article class={`rounded-2xl border border-slate-100 bg-white p-6 shadow-sm ${className}`}>
  <slot />
</article>